$(function () {
    // Give names to class to use for jQuery 
    var container = $('.container');
    var superman = $('.superman');
    var column = $('.column');
    var columnOne = $('#column-one);
    var columnTwo = $('#column-two');
    var score = $('.score');
    var reset = $('.reset');

    
});